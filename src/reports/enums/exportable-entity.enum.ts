export enum ExportableEntity {
  USERS = 'users',
  CLASSES = 'classes',
  ENROLLMENTS = 'enrollments',
  ATTENDANCE = 'attendance',
  GRADES = 'grades',
  TASKS = 'tasks',
  INCIDENTS = 'incidents',
  DOCUMENTS = 'documents'
}
